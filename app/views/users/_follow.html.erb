<%= form_with(model: current_user.active_friendships.build, local: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit "Follow", class: "btn btn-info text-white btn-block font-weight-bold" %>
<% end %>
